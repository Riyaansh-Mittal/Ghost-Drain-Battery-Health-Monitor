<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <!-- Battery -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />


    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />


    <!-- Exact alarms (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />


    <!-- Wake lock so alarm fires on screen off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <!-- Usage stats for deep sleep detection -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />


    <!-- Battery optimization exemption dialog -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />


    <!-- Required on Android 11+ so resolveActivity() can see OEM packages.
         Without this block, packageManager.resolveActivity() returns null for
         all OEM settings activities even when they are installed, causing the
         helper to silently fall through to the generic battery page. -->
    <queries>
        <!-- Samsung / OneUI -->
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.samsung.android.sm.battery" />
        <package android:name="com.samsung.android.sm" />


        <!-- Xiaomi / Redmi / POCO — MIUI / HyperOS -->
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />


        <!-- Realme / OPPO / ColorOS -->
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.coloros.oppoguardelf" />


        <!-- OnePlus / OxygenOS -->
        <package android:name="com.oneplus.security" />


        <!-- Huawei / Honor / EMUI -->
        <package android:name="com.huawei.systemmanager" />


        <!-- Vivo / iQOO — FuntouchOS / OriginOS -->
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />


        <!-- Meizu / Flyme -->
        <package android:name="com.meizu.safe" />


        <!-- Asus / ZenUI -->
        <package android:name="com.asus.mobilemanager" />


        <!-- Sony / Xperia -->
        <package android:name="com.sonymobile.cta" />


        <!-- Nokia / HMD -->
        <package android:name="com.evenwell.powersaving.g3" />


        <!-- Lenovo / ZUI -->
        <package android:name="com.lenovo.security" />
    </queries>


    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Battery Health Monitor"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BatteryHealthMonitor">


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- Foreground service -->
        <service
            android:name=".service.BatteryMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="battery_monitor" />
        </service>


        <!-- Boot receiver — restart service after reboot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>


    </application>


</manifest>